<UserControl x:Class="TARKIT.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TARKIT.Views"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="800">
    <Grid Background="#FAFAFA" Margin="0">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="500">
            <!-- Title -->
            <TextBlock Text="TARKIT" FontSize="36" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10" Foreground="#1A1A1A"/>
            
            <!-- Version -->
            <TextBlock Text="{local:Translate ui, title.main}" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,20" Foreground="#666666"/>
            
            <!-- Description -->
            <TextBlock Text="{local:Translate ui, about.description}" FontSize="13" HorizontalAlignment="Center" Margin="0,0,0,10" Foreground="#333333" TextAlignment="Center"/>
            <TextBlock Text="{local:Translate ui, about.subtext}" FontSize="12" HorizontalAlignment="Center" Margin="0,0,0,10" Foreground="#666666" TextAlignment="Center"/>
            
            <!-- Separator -->
            <Border Height="1" Background="#E0E0E0" Margin="0,0,0,10"/>
            
            <!-- Features -->
            <TextBlock Text="{local:Translate ui, section.features}" FontSize="13" FontWeight="Bold" Margin="0,0,0,5" Foreground="#1A1A1A"/>
            <StackPanel Margin="20,0,0,20">
                <TextBlock Text="{local:Translate ui, about.feature1}" FontSize="11" Margin="0,0,0,5" Foreground="#333333"/>
                <TextBlock Text="{local:Translate ui, about.feature2}" FontSize="11" Margin="0,0,0,5" Foreground="#333333"/>
                <TextBlock Text="{local:Translate ui, about.feature3}" FontSize="11" Margin="0,0,0,5" Foreground="#333333"/>
                <TextBlock Text="{local:Translate ui, about.feature4}" FontSize="11" Margin="0,0,0,5" Foreground="#333333"/>
                <TextBlock Text="{local:Translate ui, about.feature5}" FontSize="11" Margin="0,0,0,5" Foreground="#333333"/>
            </StackPanel>
            
            <!-- Separator -->
            <Border Height="1" Background="#E0E0E0" Margin="0,0,0,5"/>
            
            <!-- Planned Features -->
            <TextBlock Text="{local:Translate ui, section.planned_features}" FontSize="13" FontWeight="Bold" Margin="0,0,0,10" Foreground="#1A1A1A"/>
            <StackPanel Margin="20,0,0,10">
                <TextBlock Text="{local:Translate ui, about.planned1}" FontSize="11" Margin="0,0,0,5" Foreground="#333333"/>
                <TextBlock Text="{local:Translate ui, about.planned2}" FontSize="11" Margin="0,0,0,5" Foreground="#333333"/>
                <TextBlock Text="{local:Translate ui, about.planned3}" FontSize="11" Margin="0,0,0,5" Foreground="#333333"/>
            </StackPanel>
            
            <!-- Separator -->
            <Border Height="1" Background="#E0E0E0" Margin="0,0,0,5"/>
            
            <!-- Links -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,0">
                <Button Content="🔗 GitHub" Click="GitHub_Click" Padding="12,8" Margin="0,0,10,0" Background="#24292E" Foreground="White" BorderThickness="0" Cursor="Hand" FontSize="12"/>
                <Button Content="❤ Patreon" Click="Patreon_Click" Padding="12,8" Margin="10,0,0,0" Background="#FF424D" Foreground="White" BorderThickness="0" Cursor="Hand" FontSize="12"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
