<UserControl x:Class="TARKIT.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TARKIT.Views"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="800"
             PreviewKeyDown="UserControl_PreviewKeyDown">
    <Grid Margin="10" Focusable="True">
        <TabControl>
            <!-- Hotkeys Tab -->
            <TabItem Padding="5">
                <TabItem.Header>
                    <TextBlock Text="{local:Translate ui, section.hotkeys}"/>
                </TabItem.Header>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        <TextBlock Text="{local:Translate ui, label.hotkey_config}" FontSize="14" FontWeight="Bold" Margin="0,0,0,10"/>
                        <TextBlock Text="{local:Translate ui, message.hotkey_requirement}" FontSize="11" Foreground="#666666" Margin="0,0,0,15"/>
                        
                        <ItemsControl ItemsSource="{Binding HotKeyDisplays}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderThickness="0,0,0,1" BorderBrush="#E0E0E0" Padding="0,10,0,10" Margin="0,0,0,5">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="180"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="80"/>
                                                <ColumnDefinition Width="80"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Action}" VerticalAlignment="Center" FontWeight="Medium"/>
                                            <TextBox Grid.Column="1" Text="{Binding KeyString}" IsReadOnly="True" Margin="15,0,0,0" Padding="8" Background="#F5F5F5" BorderBrush="#CCCCCC"/>
                                            <Button Grid.Column="2" Content="{local:Translate ui, button.bind}" Command="{Binding DataContext.BindHotKeyCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Margin="10,0,5,0" Padding="6"/>
                                            <Button Grid.Column="3" Content="{local:Translate ui, button.clear}" Command="{Binding DataContext.ClearHotKeyCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Margin="5,0,0,0" Padding="6"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        
                        <Border BorderThickness="0,1,0,0" BorderBrush="#CCCCCC" Margin="0,15,0,15"/>
                        
                        <!-- Listening Status -->
                        <Border Visibility="{Binding IsListeningForHotKey, Converter={StaticResource BoolToVisibilityConverter}}" Background="#E3F2FD" Padding="10" Margin="0,0,0,10" BorderThickness="1" BorderBrush="#B3E5FC">
                            <StackPanel>
                                <TextBlock Text="{local:Translate ui, message.listening_for_hotkey}" FontSize="12" FontWeight="Bold" Foreground="#0078D4" Margin="0,0,0,5"/>
                                <TextBlock Text="{local:Translate ui, message.press_key_combination}" FontSize="11" Foreground="#0078D4"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Instructions -->
                        <StackPanel>
                            <TextBlock Text="{local:Translate ui, instruction.step1}" FontSize="11" FontWeight="Bold" Margin="0,0,0,5"/>
                            <TextBlock Text="{local:Translate ui, instruction.step1}" FontSize="10" Foreground="#666666" Margin="0,0,0,3"/>
                            <TextBlock Text="{local:Translate ui, instruction.step2}" FontSize="10" Foreground="#666666" Margin="0,0,0,3"/>
                            <TextBlock Text="{local:Translate ui, instruction.step3}" FontSize="10" Foreground="#666666" Margin="0,0,0,3"/>
                            <TextBlock Text="{local:Translate ui, instruction.step4}" FontSize="10" Foreground="#666666"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Language Tab -->
            <TabItem Padding="5">
                <TabItem.Header>
                    <TextBlock Text="{local:Translate ui, section.language}"/>
                </TabItem.Header>
                <StackPanel Margin="10" VerticalAlignment="Top">
                    <TextBlock Text="{local:Translate ui, message.language_settings}" FontSize="14" FontWeight="Bold" Margin="0,0,0,10"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                        <TextBlock Text="{local:Translate ui, label.language}" VerticalAlignment="Center" Margin="0,0,10,0" Width="120"/>
                        <ComboBox ItemsSource="{Binding AvailableLanguages}" SelectedItem="{Binding SelectedLanguage}" Width="150" Padding="5"/>
                    </StackPanel>
                    <TextBlock Text="{local:Translate ui, message.language_update}" FontSize="11" Foreground="#666666"/>
                </StackPanel>
            </TabItem>

            <!-- Overlay Tab -->
            <TabItem Padding="5">
                <TabItem.Header>
                    <TextBlock Text="{local:Translate ui, section.overlay}"/>
                </TabItem.Header>
                <StackPanel Margin="10" VerticalAlignment="Top">
                    <TextBlock Text="{local:Translate ui, message.overlay_settings}" FontSize="14" FontWeight="Bold" Margin="0,0,0,15"/>
                    <TextBlock Text="{local:Translate ui, label.overlay_transparency}" Margin="0,0,0,5"/>
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>
                        <Slider Grid.Column="0" Minimum="0" Maximum="100" Value="{Binding OverlayTransparency}" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="10,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding OverlayTransparency}"/>
                            <TextBlock Text="%"/>
                        </StackPanel>
                    </Grid>
                    <TextBlock Text="{local:Translate ui, message.transparency_info}" FontSize="11" Foreground="#666666"/>
                </StackPanel>
            </TabItem>

            <!-- File Paths Tab -->
            <TabItem Padding="5">
                <TabItem.Header>
                    <TextBlock Text="{local:Translate ui, section.file_paths}"/>
                </TabItem.Header>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        <TextBlock Text="{local:Translate ui, message.file_paths}" FontSize="14" FontWeight="Bold" Margin="0,0,0,15"/>
                        
                        <!-- Quest Data Path -->
                        <TextBlock Text="{local:Translate ui, label.quest_data_path}" Margin="0,0,0,5"/>
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="80"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" Text="{Binding QuestDataPath}" Padding="5" IsReadOnly="False"/>
                            <Button Grid.Column="1" Content="{local:Translate ui, button.browse}" Command="{Binding BrowseQuestDataCommand}" Margin="10,0,0,0" Padding="5"/>
                        </Grid>

                        <!-- Item Data Path -->
                        <TextBlock Text="{local:Translate ui, label.item_data_path}" Margin="0,0,0,5"/>
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="80"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" Text="{Binding ItemDataPath}" Padding="5" IsReadOnly="False"/>
                            <Button Grid.Column="1" Content="{local:Translate ui, button.browse}" Command="{Binding BrowseItemDataCommand}" Margin="10,0,0,0" Padding="5"/>
                        </Grid>

                        <!-- User Data Path -->
                        <TextBlock Text="{local:Translate ui, label.user_data_path}" Margin="0,0,0,5"/>
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="80"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" Text="{Binding UserDataPath}" Padding="5" IsReadOnly="False"/>
                            <Button Grid.Column="1" Content="{local:Translate ui, button.browse}" Command="{Binding BrowseUserDataCommand}" Margin="10,0,0,0" Padding="5"/>
                        </Grid>

                        <Border BorderThickness="0,1,0,0" BorderBrush="#CCCCCC" Margin="0,10,0,10"/>
                        <TextBlock Text="{local:Translate ui, message.leave_paths_empty}" FontSize="11" Foreground="#666666" Margin="0,0,0,20"/>
                        
                        <Button Content="{local:Translate ui, button.save}" Command="{Binding SaveSettingsCommand}" Width="120" Padding="5"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Exit Tracker Tab -->
            <TabItem Padding="5">
                <TabItem.Header>
                    <TextBlock Text="Exit Tracker"/>
                </TabItem.Header>
                <StackPanel Margin="10" VerticalAlignment="Top">
                    <TextBlock Text="Exit Tracking Settings" FontSize="14" FontWeight="Bold" Margin="0,0,0,15"/>
                    
                    <CheckBox 
                        IsChecked="{Binding EnableExitTracker}" 
                        Margin="0,0,0,10"
                        Padding="5">
                        <TextBlock Text="Enable Exit Tracker (OCR)" VerticalAlignment="Center"/>
                    </CheckBox>
                    
                    <TextBlock 
                        Text="Automatically detects available raid exits using screen capture and OCR." 
                        FontSize="11" 
                        Foreground="#666666"
                        TextWrapping="Wrap"
                        Margin="0,0,0,10"/>
                    
                    <Border Background="#FFF9C4" Padding="10" Margin="0,10,0,10" BorderThickness="1" BorderBrush="#FBC02D">
                        <StackPanel>
                            <TextBlock Text="Note:" FontWeight="Bold" Margin="0,0,0,5"/>
                            <TextBlock Text="• Requires tessdata files in application directory" FontSize="10"/>
                            <TextBlock Text="• Runs on background thread with low priority" FontSize="10"/>
                            <TextBlock Text="• Captures top-right screen region every 2 seconds" FontSize="10"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
