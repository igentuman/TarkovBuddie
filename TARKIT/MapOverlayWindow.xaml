<Window x:Class="TARKIT.MapOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Map Overlay" 
        WindowState="Maximized"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        ShowInTaskbar="False"
        Topmost="True">
    <Grid Background="Transparent">
        <Canvas x:Name="MapCanvas" 
                Background="Transparent"
                MouseWheel="MapCanvas_MouseWheel"
                MouseLeftButtonDown="MapCanvas_MouseLeftButtonDown"
                MouseMove="MapCanvas_MouseMove"
                MouseLeftButtonUp="MapCanvas_MouseLeftButtonUp">
            <Image x:Name="MapImage" 
                   Source="{Binding CurrentMapImage}"
                   Stretch="UniformToFill"
                   Width="{Binding MapWidth}"
                   Height="{Binding MapHeight}"/>
        </Canvas>
        
        <Border VerticalAlignment="Top" HorizontalAlignment="Right" Margin="10" Background="#CC000000" Padding="10" CornerRadius="4">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="Map Overlay" Foreground="White" FontSize="14" FontWeight="Bold" Margin="0,0,0,10"/>
                <TextBlock Text="Scroll: Zoom | Drag: Pan" Foreground="#CCCCCC" FontSize="11" Margin="0,0,0,10"/>
                <Button Content="Close (ESC)" Click="Close_Click" Padding="8,5" Width="120"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
