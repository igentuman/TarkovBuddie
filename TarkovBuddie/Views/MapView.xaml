<UserControl x:Class="TarkovBuddie.Views.MapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TarkovBuddie.Views"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Canvas x:Name="MapCanvas" 
                Grid.Row="0"
                Background="#F0F0F0"
                MouseWheel="MapCanvas_MouseWheel"
                MouseLeftButtonDown="MapCanvas_MouseLeftButtonDown"
                MouseMove="MapCanvas_MouseMove"
                MouseLeftButtonUp="MapCanvas_MouseLeftButtonUp">
            <Image x:Name="MapImage" 
                   Source="{Binding CurrentMapImage}"
                   Stretch="Uniform"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>
        </Canvas>

        <Border Grid.Row="1" Background="#F5F5F5" BorderThickness="0,1,0,0" BorderBrush="#CCCCCC" Padding="10">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Controls:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Medium" FontSize="12"/>
                <TextBlock Text="Scroll: Zoom" VerticalAlignment="Center" Margin="0,0,20,0" FontSize="11" Foreground="#666666"/>
                <TextBlock Text="Drag: Pan" VerticalAlignment="Center" Margin="0,0,20,0" FontSize="11" Foreground="#666666"/>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Zoom: " VerticalAlignment="Center" FontSize="11" Foreground="#666666"/>
                    <TextBlock Text="{Binding ZoomLevel, StringFormat=F2}" VerticalAlignment="Center" FontSize="11" Foreground="#0078D4" FontWeight="Bold"/>
                    <TextBlock Text="x" VerticalAlignment="Center" FontSize="11" Foreground="#0078D4" FontWeight="Bold"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
